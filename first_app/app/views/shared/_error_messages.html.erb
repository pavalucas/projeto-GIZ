<% if object.errors.any? %>
    <div class="alert alert-danger">
        O formulário contém <%= pluralize(object.errors.count, "erros") %>.
      <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
<% end %>